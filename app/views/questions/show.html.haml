%h4= @question.title

= link_to 'Follow User', follow_user_path(user_id: @question.author.id), class: 'follow_user btn btn-info btn-sm'
= link_to 'Follow Topic', follow_topic_path(topic_id: @question.topic.id), class: 'follow_topic btn btn-info btn-sm'

%br
%br

Your Answer

= form_for Answer.new, url: answer_question_path(@question) do |f|
  .form-group
    = f.text_area :ans, autofocus: true, autocomplete: :off, size: '50x5', required: true, class: 'form-control'
  .actions
    = f.submit "Post Answer", class: 'btn btn-primary btn-sm'

%br
%br

- @question.answers.each do |ans|
  .card
    %ul.list-group.list-group-flush
      %li.list-group-item
        .answer= ans.ans
